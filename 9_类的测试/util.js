class Util {
    init() {
        // 超级复杂,我只想知道是否执行了，不想知道结果，因为耗性能，怎么办呢？
    }
    a() {
        // 超级复杂,我只想知道是否执行了，不想知道结果，因为耗性能，怎么办呢？
    }
    b() {
        // 超级复杂,我只想知道是否执行了，不想知道结果，因为耗性能，怎么办呢？
    }
}

export default Util